// Criar um vetor com 8 posições
var vetor = new Array(8);

// Preencher o vetor com o número 999
for (var i = 0; i < vetor.length; i++) {
    vetor[i] = 999;
}

// Exibir o vetor e seus índices
for (var i = 0; i < vetor.length; i++) {
    console.log("índice=" + i + " número=" + vetor[i]);
}
